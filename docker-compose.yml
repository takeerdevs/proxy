version: "3.8"
services:
  squid:
    build: .
    container_name: squid-proxy
    ports:
      - "3128:3128"
    volumes:
      - squid-cache:/var/spool/squid
    restart: unless-stopped

volumes:
  squid-cache:
